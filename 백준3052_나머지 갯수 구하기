arr = []
for i in range(10):
    n = int(input())
    arr.append(n % 42)
arr = set(arr)
print(len(arr))
우선 빈 배열을 만들어 준다.

 
"""
n을 입력받은 뒤, append를 이용하여 배열의 마지막에 원소를 추가해준다.

 

입력받은 값을 42로 나눈 나머지들이 arr에 저장된다.

 

그 arr를 set으로 집합 자료형으로 만들어준다.

 

(set함수는 간단하게 말하면, 중복을 제거하기 위한 필터역할을 해준다.)

 

그리고 arr의 len, 길이를 출력해준다.
"""
